name: GitLeaks
on:
  pull_request:
  push:

permissions:
  contents: read
  pull-requests: write

jobs:
  scan:
    name: gitleaks
    uses: Urjanet/gitleaks-workflow/.github/workflows/gitleaks.yaml@main
    secrets: inherit
